export enum MaritalState {
  SINGLE = 'solteiro(a)',
  MARRIED = 'casado(a)',
  WIDOWED = 'viúvo(a)',
  DIVORCED = 'divorciado(a)',
  SEPARATED = 'separado(a)',
  LEGAL_PARTNERSHIP = 'união estável',
  NI = 'não informado',
}
